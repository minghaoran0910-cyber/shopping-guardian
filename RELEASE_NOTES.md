# 购物守护者 v1.8.7

## 本次更新

- 购物车截图或购物清单识别出多件商品后，改为逐件填写理由、预算、分类和标签；
- 每件商品分别请求模型、分别形成决定和历史记录，不再把耳机、唱片等不同商品混成一条建议；
- 导入预览支持排除不想分析的商品；
- 中途返回不会丢掉尚未处理的商品，已保存的决定也不会回滚；
- 新增“已有物品、历史订单、价格证据、确认偏好、消费人格分享”分阶段路线图。

## 验收

- 149 项自动化测试通过；
- Dart 静态检查 0 问题；
- 导入预览 Golden 基线经人工检查并更新；
- Android、iOS Simulator 和 macOS 构建结果见本版本提交；
- Windows 构建由本次推送后的 GitHub Actions 复验。

---

# 购物守护者 v1.8.6

## 本次更新

- 修复拼多多详情截图被 Android ML Kit 分块识别时，“店铺 / 收藏 / 客服 / 品牌”等导航文字混入商品标题的问题；
- 新增真实分块顺序回归样本，保留商品品牌、规格和主体描述；
- 使用同一张用户截图在 Android ML Kit 与 iOS Vision 上复验，均识别出干净标题、拼多多平台和 ¥12 价格；
- 使用 `qwen3.5-flash` 按量端点完成 iOS 1.8.5/1.8.6 配置连接和真实购买建议闭环验收。

## 验收

- 149 项自动化测试通过；
- Dart 静态检查 0 问题；
- Android Debug、iOS Simulator Debug、macOS Debug 构建通过；
- Windows 构建由本次推送后的 GitHub Actions 复验。

---

# 购物守护者 v1.8.4

## 本次更新

- 将“稍后再看”“记录”“习惯”“设置”四个完整页面从 `home_shell.dart` 移入独立源码文件；
- 主页面文件从 3404 行降至 1428 行，不改变现有界面和业务流程；
- 保留同一 Dart library 的私有边界，避免本轮结构调整引入新的公共 API；
- 延续 v1.8.3 的淘宝、京东、拼多多截图识别修复。
- 模型设置接受常见的 OpenAI 兼容 Base URL；以 `/v1` 结尾时自动补全 `/chat/completions`。
- 成功保存购买决定后清空首页草稿，避免误点造成重复分析；失败或取消时仍保留输入。

## 验收

- 设置、导入预览、浅色/暗色 Golden 基线无像素变化；
- Widget 与无障碍定向测试通过；
- 完整测试、平台构建与 CI 结果见本版本提交。
- 使用真实百炼按量付费模型完成发送预览、结构化分析、四种决策入口、冷静期提醒和历史落库验收。

---

# 购物守护者 v1.3.1

## 本次更新

- 浅色主题改为白色与中性灰，暗色主题改为黑色与深灰；
- 移除绿色和砖红色装饰性强调色，主操作统一使用黑白高对比；
- 新增暗黑主题视觉回归基线；
- 设置页新增“清除全部本地数据”；
- 完整清除包含决策、预算、规则、API 配置、主题、语言、首次启动状态和已安排提醒；
- Android 记录本应用排程的提醒 ID，完整清除时逐项取消；
- iOS 与 macOS 支持一次取消所有待发送和已送达提醒；
- 新增完整清除自动化测试；
- 保存 v1.3.0 审查报告和后续开发路线图；
- README 三张 Android 截图已换成当前黑白主题。

## 验收

- `dart analyze lib test integration_test tool`：通过；
- `flutter test`：59 项通过；
- macOS Release、Android Release APK、iOS Simulator 构建：通过；
- Windows 由既有 GitHub Actions 在推送后复验。

---

# 购物守护者 v1.1.0

Android 首个可安装版本。现在可以直接从淘宝、天猫或京东的系统分享面板导入链接，也可以在设备上识别购物车截图。

## Android 新增

- 接收系统分享的商品文字和链接
- 支持应用冷启动和已打开状态下的新分享
- 系统文件选择器，不读取整个相册
- 随 APK 提供的 ML Kit 中文 OCR，识别不依赖用户模型
- Android Keystore 支持的 API Key 安全存储
- 冷静期本地通知，支持创建和取消
- Android 13 及以上按需请求通知权限

## 现有功能

- 淘宝、天猫单商品分享链接
- 京东单商品和购物清单分享链接
- 淘宝、天猫、京东购物车截图，本地 OCR
- 手动商品录入和导入结果编辑
- JustOneAPI 商品详情补全
- OpenAI-compatible 模型配置、连接测试和结构化建议
- 月度非必要消费预算
- 可新增、启停和删除的个人消费规则
- 购买、等待、放弃、寻找替代四种决定
- 冷静期页面和本地通知
- 本地历史搜索、筛选、详情、反馈和删除
- 基于真实记录的基础习惯统计
- 不含 API Key 的 JSON 导出
- 本地业务数据与 API 配置独立清除

## Android 安装提示

APK 直接从 GitHub 下载，没有通过 Google Play 发布。首次安装时，Android 会询问是否允许当前文件管理器安装未知应用。

APK SHA-256：`576a8d8336833e4499dd9526b74f701d2adecdd850d00de5acb89db4b516a55c`

## 已知限制

- 淘宝购物车分享链接可能只允许淘宝 App 打开，建议使用购物车截图。
- OCR 结果受截图清晰度和页面排版影响，分析前请核对。
- iOS 安装包尚未发布。
- Android 系统分享目前只接收文字和链接，截图需在应用内选择。
